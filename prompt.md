# Codex 最終実装プロンプト

あなたは、GitHub リポジトリ `abigworld1/abigworld1.github.io` で作業するシニア・フロントエンド／ゲームエンジニアです。

`https://abigworld1.github.io/sliding-block-puzzle/` で、スマートフォンと PC の両方から遊べる **独立制作の「4×5 脱出スライドパズル」** を完成させてください。

計画の提示だけで止まらず、実際のファイル作成、実装、テスト、修正まで行い、GitHub Pages に置けば動く完成状態にしてください。

---

## 1. 最優先事項

1. 原則として新規の `sliding-block-puzzle/` ディレクトリ内だけを変更し、既存サイトのページ、CSS、JavaScript、設定を壊さないこと。
2. サーバー、データベース、外部 API、CDN、本番時のパッケージインストールを必要としない静的サイトにすること。
3. `index.html`、CSS、JavaScript ES Modules で動作させ、GitHub Pages の `/sliding-block-puzzle/` 配下で相対パスだけを使うこと。
4. スマートフォンのタッチ操作、PC のマウス操作、ペン操作、キーボード操作に対応すること。
5. 盤面ロジックを DOM や描画処理から分離し、純粋関数として単体テスト可能にすること。
6. コア機能に `TODO`、ダミーボタン、未接続 UI、見せかけのヒント機能を残さないこと。
7. 途中で確認待ちにならず、合理的に判断して実装を進めること。重大な不明点は、安全側かつ単純な仕様を選び README に記録すること。
8. 実装終了時に、変更ファイル一覧、テスト結果、ローカル起動方法、GitHub Pages での確認方法、既知の制約を報告すること。

---

## 2. 著作権・商標・デザインに関する必須制約

公開物は、特定メーカー製品の公式移植、複製、互換版ではなく、古典的な 4×5 スライディングブロックの仕組みを使った独立制作物として扱ってください。

次を厳守してください。

- 公開タイトルは **「4×5 脱出パズル」** とする。
- HTML の `title`、見出し、OG 情報、favicon、説明文に、特定メーカー名、製品名、ロゴを使用しない。
- 公式ロゴ、商品写真、説明書画像、動画フレーム、QR コード、ストーリー文章、説明文、図版、解法図、音声を転載・トレース・埋め込み・同梱しない。
- 公式サイトや動画から画像をダウンロードして素材化しない。
- 実物の木目、赤・黄・青の円、印刷位置などを忠実に再現しない。外観は、フラットな幾何学デザイン、独自配色、独自アイコンで新規制作する。
- 特定製品固有の 21 問の配置や 79 ステップの解答を、説明書や画像から転記しない。
- 本番 UI には次の趣旨の短い注記を表示する。

  `本作は独立制作の非公式なスライドパズルです。特定の製品・企業との提携・承認関係はありません。`

- README にも同趣旨を記載する。
- 参考資料への外部リンクを本番 UI に置く必要はない。開発メモに記載する場合も、素材や文章を転載しないことを明記する。

この制約と他の要件が衝突した場合は、この制約を優先してください。

---

## 3. ローカル参考資料の扱いと push 前の削除

作業開始時点では、実装のヒントとして次のローカル参考資料が `sliding-block-puzzle/` に置かれています。

- `IMG_4440.jpg`: 実物の盤面と駒を撮影した写真。
- `IMG_4442.jpg`: 説明書の前半を撮影した画像（ルール、開始／終了配置例、解法図の前半を含む）。
- `IMG_4443.jpg`: 説明書の後半を撮影した画像（解法図の後半を含む）。
- `TDM14.pdf`: 2 ページの説明書 PDF（ルール、配置例、解法図を含む）。

依頼時に「3 つの PNG ファイル」と表現された画像資料は、現在のリポジトリでは上記 3 つの JPEG ファイルです。作業時に拡張子やファイル名が変わっている場合は、同内容の画像 3 点を対象として扱ってください。

これらは公開用アセットではなく、**実装中だけ参照できる一時的な資料**です。次の方針を必ず守ってください。

- 4×5 の盤面、駒を持ち上げずにスライドする基本操作、下辺中央から目標駒を出す目的など、一般的なゲーム構造を理解するためのヒントとしてのみ使う。
- 写真、PDF、サムネイル、切り抜き、OCR した文章、ロゴ、QR コード、図版を、公開ファイル、テスト fixture、README、コメントなどへ転載・埋め込み・トレースしない。
- 資料固有の外観、配色、名称、物語、10 組の開始／終了配置、手順 1〜79 の解法を転記しない。初期配置は本プロンプトの独立したレベル定義だけを正とし、解答はソルバーで生成する。
- HTML、CSS、JavaScript、manifest、README などから、これらのローカル資料を参照・リンク・import しない。
- 資料を Git LFS へ移したり、別名・別形式へ変換してリポジトリに残したりしない。
- 実装とテストが完了したら、**`git push` を行う前に画像 3 点と PDF 1 点を作業ツリーから削除する**。
- 削除後に `git status` と追跡対象ファイルを確認し、4 点の原本・変換物・派生物が push 対象に残っていないことを確認する。すでに Git で追跡されている場合は、削除自体をコミット対象に含める。
- 最終報告には、4 点を実装のヒントとしてのみ参照し、公開物へ転用せず、push 前に削除済みであることを記載する。

---

## 4. 完成条件

以下をすべて満たすことを MVP の完成条件とします。

- 4 列×5 行の盤面で駒を上下左右にスライドできる。
- 駒は回転せず、他の駒や盤面境界を通過しない。
- 2×2 の目標駒を下中央の出口から外へ出すとクリアになる。
- Pointer Events により、タッチ、マウス、ペンで滑らかにドラッグできる。
- タップ＋方向ボタン、キーボードでも完全に遊べる。
- Undo、Redo、Restart が正しく動く。
- 手数、経過時間、クリア表示、ベスト記録が動く。
- ページ再読み込み後に進行状態を復元できる。
- 現在局面から合法な解答を求めるヒント機能が実際に動く。
- 解答を 1 手ずつ再生、または自動再生できる。
- 盤面ロジックとソルバーのテストが通る。
- 画面幅 320px 程度のスマートフォンからデスクトップまで崩れない。
- キーボードのみでも操作可能で、`prefers-reduced-motion` に対応する。
- `https://abigworld1.github.io/sliding-block-puzzle/` 配下で 404 や絶対パス不具合を起こさない。

---

## 5. ゲーム仕様

### 5.1 盤面

- 盤面は 4 列×5 行。
- 左上を原点とし、`x = 0..3`、`y = 0..4`。
- 下辺中央の `x = 1..2` が 2 セル幅の出口。
- 盤面の総セル数は 20。
- 駒の占有セル数は 18 で、通常状態では空きセルが 2 つ。

### 5.2 駒構成

- 目標駒 `K`: 1 個、2×2。
- 縦長駒 `V1..V4`: 4 個、1×2。
- 横長駒 `H1`: 1 個、2×1。
- 小駒 `S1..S4`: 4 個、1×1。
- すべての駒は回転不可。

見た目は独自に設計してください。例として、目標駒はアクセント色、縦長駒・横長駒・小駒は形状と模様の差で識別し、色だけに依存しないようにします。

### 5.3 初期配置

次の一般的なクラシック配置を使用してください。

```js
export const CLASSIC_LEVEL = {
  id: "classic-escape",
  name: "クラシック",
  mode: "escape",
  board: {
    cols: 4,
    rows: 5,
    exit: { edge: "bottom", x: 1, width: 2 }
  },
  pieces: [
    { id: "K",  kind: "target",   x: 1, y: 0, w: 2, h: 2 },
    { id: "V1", kind: "domino-v", x: 0, y: 0, w: 1, h: 2 },
    { id: "V2", kind: "domino-v", x: 3, y: 0, w: 1, h: 2 },
    { id: "H1", kind: "domino-h", x: 1, y: 2, w: 2, h: 1 },
    { id: "V3", kind: "domino-v", x: 0, y: 3, w: 1, h: 2 },
    { id: "V4", kind: "domino-v", x: 3, y: 3, w: 1, h: 2 },
    { id: "S1", kind: "single",   x: 1, y: 3, w: 1, h: 1 },
    { id: "S2", kind: "single",   x: 2, y: 3, w: 1, h: 1 },
    { id: "S3", kind: "single",   x: 1, y: 4, w: 1, h: 1 },
    { id: "S4", kind: "single",   x: 2, y: 4, w: 1, h: 1 }
  ]
};
```

初期空きセルは `(0, 2)` と `(3, 2)` です。

初期状態で少なくとも次を合法手としてテストしてください。

- `H1` を左へ 1 セル。
- `H1` を右へ 1 セル。
- `V3` を上へ 1 セル。
- `V4` を上へ 1 セル。

### 5.4 移動

- 1 回の操作では 1 個の駒だけを動かす。
- 上下左右のいずれか 1 軸だけに移動する。
- 斜め移動、回転、重なり、通常駒の盤外移動は禁止。
- 1 回のドラッグで同じ方向へ複数セル動かせる。
- ドラッグ中は衝突直前で表示位置をクランプする。
- ドラッグ終了時は、最も近い合法な整数セル位置へスナップする。
- 有効移動が 0 セルなら履歴や手数に追加しない。
- 1 個の駒を 1 方向へ確定移動した操作を 1 手と数える。複数セル移動でも 1 手。
- 内部の移動表現は `{ pieceId, fromX, fromY, toX, toY }` または同等の明確な形式にする。
- すべての状態更新は、移動検証、状態更新、履歴追加、手数加算、保存、勝利判定を一貫して行う 1 つの経路に集約する。

### 5.5 クリア

- 目標駒 `K` が `x = 1, y = 3` に到達すると出口直前。
- この時点ではまだクリアにしない。
- さらに下方向へドラッグ、下方向ボタン、または下矢印キーを入力すると「脱出」アクションを実行する。
- 脱出アクションも 1 手として数える。
- 脱出後は目標駒を短く下へアニメーションさせてからクリア画面を表示する。
- `prefers-reduced-motion: reduce` の場合はアニメーションを省略または大幅に短縮する。
- 内部モデルで通常駒が恒常的に盤外座標を持たないようにし、勝利状態は明示的なフラグまたは専用状態で表す。

---

## 6. 入力仕様

### 6.1 Pointer Events

`pointerdown`、`pointermove`、`pointerup`、`pointercancel`、`setPointerCapture()` を使い、マウス・タッチ・ペンを単一実装にまとめてください。

処理手順:

1. 押下した駒を特定する。
2. 小さなデッドゾーンを超えたら水平・垂直の優勢軸を固定する。
3. 対象駒以外の占有グリッドから、固定軸方向の最大合法移動量を求める。
4. ドラッグ中は CSS transform で滑らかに描画し、合法範囲内にクランプする。
5. 離したら最寄りの合法セルへスナップする。
6. 確定移動だけをモデルへ適用する。

盤面にだけ `touch-action: none` を指定し、ページ全体の通常スクロールを不要に妨げないでください。

### 6.2 タップ操作

- 駒をタップすると選択状態になる。
- 選択中の合法方向を視覚表示する。
- 画面上の方向ボタンで 1 セル移動できる。
- 「端まで」操作、または長押し相当の UI で、その方向の最遠合法位置まで 1 手で移動できる。
- タップ領域は十分な大きさを確保する。

### 6.3 キーボード

- `Tab`: 操作可能な駒やボタンへフォーカス移動。
- `Enter` / `Space`: 駒を選択。
- 矢印キー: 選択駒を 1 セル移動。
- `Shift + 矢印`: 選択駒をその方向の最遠合法位置へ移動。
- `Ctrl/Cmd + Z`: Undo。
- `Ctrl/Cmd + Shift + Z` または `Ctrl/Cmd + Y`: Redo。
- `R`: リスタート確認。
- `H`: 次の一手ヒント。
- `Escape`: 選択解除、ダイアログを閉じる。

フォーム部品やダイアログ操作中にゲーム用ショートカットが誤作動しないようにしてください。

---

## 7. UI / UX

### 7.1 構成

- ヘッダー: タイトル、短い説明、ヘルプボタン。
- メイン: 盤面、ステータス、操作ボタン。
- ステータス: 手数、経過時間、ベスト手数、ベスト時間。
- 操作: Undo、Redo、最初から、ヒント、解答再生、音や動きの設定が必要なら最小限。
- クリア時: アクセシブルなダイアログまたはインラインパネル。
- フッター: 独立制作・非公式の注記、サイトトップへ戻るリンク。

PC では盤面と操作欄を横並びにしてよいですが、スマートフォンでは 1 カラムにしてください。

### 7.2 ビジュアル

- 写真風の木製盤面にはしない。
- 独自のフラット／幾何学デザインにする。
- 盤面は CSS Grid または絶対配置＋CSS 変数で構成し、4:5 の比率を保つ。
- 駒は色だけでなく、模様、枠、形状差、ラベルなどでも区別する。
- 出口を明確に表示する。
- 選択中、合法方向、ヒント対象、ドラッグ中、フォーカス状態を明確にする。
- 320px 程度の幅でも横スクロールなしでプレイできる。
- `clamp()`、`min()`、`max()`、CSS カスタムプロパティを適切に使う。
- ライト／ダーク両方で読みやすくする。必要なら `prefers-color-scheme` に追従する。
- フォントやアイコンを外部 CDN から読み込まない。

### 7.3 アクセシビリティ

- すべてのボタンに明確なラベルを付ける。
- 駒はキーボードフォーカス可能にする。
- 現在選択中の駒、合法方向、移動結果、クリアを `aria-live` で簡潔に通知する。
- 色の違いだけに依存しない。
- フォーカスリングを消さない。
- ダイアログを使う場合はフォーカス管理を正しく行う。
- `prefers-reduced-motion` を尊重する。

---

## 8. 状態管理、履歴、保存

### 8.1 Undo / Redo

- 確定移動単位で履歴を保持する。
- Undo 後に新しい移動を行った場合は Redo スタックを破棄する。
- Restart は初期状態へ戻す。誤操作防止の確認を表示するが、アクセシブルにする。
- ヒント表示だけでは手数や履歴を変更しない。
- 解答再生による移動も通常履歴へ入れるか、再生開始前に復元可能な方針を明確にし、一貫して実装する。

### 8.2 LocalStorage

- キー例: `abigworld.escape.v1`。
- 保存形式に `schemaVersion` を含める。
- 現在配置、手数、経過時間、履歴、Redo、クリア状態、ベスト記録を必要な範囲で保存する。
- 壊れた JSON、古いスキーマ、不正配置を検出したら安全に初期化する。
- 保存データを読み込む前に盤面バリデータを通す。
- タイマーはタブ非表示時間の扱いを一貫させる。推奨は「プレイ開始からの実経過時間」ではなく、ページがアクティブで未クリアの時間だけを加算する方式。

---

## 9. 盤面モデルとバリデーション

最低限、次の責務を分離してください。

- レベル定義。
- 状態の正規化と直列化。
- 占有グリッド生成。
- 配置バリデーション。
- 合法移動列挙。
- 指定移動の検証。
- 移動適用。
- Undo 用逆移動。
- 出口直前判定。
- 脱出可能判定。
- 勝利判定。

レベル／状態バリデータは少なくとも次を確認してください。

- 盤面サイズが正しい。
- 必須駒の個数とサイズが正しい。
- 座標が整数。
- 通常状態の駒が盤面内。
- 駒同士が重ならない。
- 占有セル数が想定どおり。
- 出口定義が正しい。
- 不明な駒種や重複 ID がない。

DOM 上の座標をゲーム状態の真実にせず、モデル状態から描画してください。

---

## 10. ヒントとソルバー

固定の解答配列を埋め込むのではなく、現在局面から合法な解答を探索してください。

### 10.1 実装方針

- Web Worker 内で BFS、または正しさを保てる同等の探索を実装する。
- UI スレッドを長時間ブロックしない。
- 新しい探索開始時に古い探索をキャンセルまたは無視できるよう、リクエスト ID を使う。
- 状態はコンパクトな文字列または整数列へ直列化する。
- 同形駒の入れ替えで状態数が増えすぎないよう、縦長駒群と小駒群を位置順に正規化する。
- 正規化状態から復元する解答手は、固定 ID だけに依存せず、`kind`、移動前座標、移動後座標などで現在の実駒に対応付ける。
- 1 手は「1 個の駒を 1 方向へ 1 セル以上動かす操作」とする。
- ある方向へ移動可能なすべての停止位置を探索候補に含める。
- 目標駒が出口直前に到達した後の「脱出」を終端アクションとして扱う。

### 10.2 UI

- 「ヒント」は解答の次の 1 手だけを強調表示する。
- ヒント表示だけでは自動的に駒を動かさない。
- 「1 手進める」で解答の次の手を実行できる。
- 「自動再生」は適度な間隔で解答を実行し、一時停止・中止できる。
- ユーザーが手動操作したら、古い解答列を破棄して再探索する。
- 探索中、解答なし、キャンセル、エラーを明確に表示する。
- クラシック初期配置については、探索が実用時間内で完了し、最終的にクリアへ到達することをテストする。

---

## 11. 推奨ファイル構成

既存リポジトリを確認し、より適切な構成がなければ次を基準にしてください。

```text
sliding-block-puzzle/
  index.html
  styles.css
  README.md
  js/
    app.js
    constants.js
    levels.js
    model.js
    history.js
    storage.js
    timer.js
    input.js
    renderer.js
    solver-client.js
    solver-worker.js
  tests/
    model.test.js
    solver.test.js
    storage.test.js
```

責務が明確ならファイル数は調整して構いません。ただし、1 ファイルへすべて詰め込まないでください。

テストは Node.js 標準の `node:test` と `node:assert/strict` を使い、本番コードに外部依存を導入しないでください。必要であれば `sliding-block-puzzle/package.json` に `"type": "module"` とテストスクリプトだけを置いて構いません。

---

## 12. 必須テスト

最低限、次を自動テストしてください。

1. 初期配置がバリデーションを通る。
2. 初期空きセルが `(0,2)` と `(3,2)`。
3. 初期状態で `H1` 左、`H1` 右、`V3` 上、`V4` 上が合法。
4. 他駒との重なり、盤面外、斜め移動、回転相当の不正移動が拒否される。
5. 複数セル移動の途中経路に障害物があれば拒否される。
6. 適用した移動を逆移動すると元状態へ戻る。
7. Undo / Redo の状態遷移が正しい。
8. 直列化と復元で同じ状態になる。
9. 同形駒の ID 入れ替えがソルバーの正規化状態では同一になる。
10. 目標駒が出口以外から盤外へ出られない。
11. 出口直前でのみ脱出アクションが合法。
12. ソルバーがクラシック初期配置から解答を返す。
13. ソルバーの返した全手が順に合法で、適用後に勝利状態へ到達する。
14. 壊れた保存データや不正状態が安全に拒否される。

ソルバーの最短手数を、外部資料の数字に合わせて固定アサートしないでください。アプリ独自の「1 操作 = 1 手」の定義で正しく解けることを確認してください。

---

## 13. GitHub Pages 対応

- `/sliding-block-puzzle/` の絶対パスをハードコードせず、`./styles.css`、`./js/app.js` のような相対パスを使う。
- JavaScript Worker の URL は `new URL("./solver-worker.js", import.meta.url)` のように解決する。
- ローカルファイル直開きではなく HTTP サーバー経由で確認する。
- README に次を記載する。
  - 例: `python3 -m http.server 8000` をリポジトリルートで実行。
  - `http://localhost:8000/sliding-block-puzzle/` を開く。
  - テスト実行方法。
  - GitHub Pages 反映後の確認 URL。
- 既存の GitHub Pages 設定やルートのファイルを、必要性が確認できないまま変更しない。

---

## 14. 任意拡張

MVP が完成し、すべてのテストが通った後に限り、次を追加して構いません。

- 既知の解ける状態からランダムな合法手を逆向きにたどって作る「チャレンジ」配置。
- シード付きの再現可能な問題生成。
- 生成後にソルバーで解けることを検証。
- 問題共有用の短い URL パラメータ。
- オフライン対応用の最小限の Service Worker。

ただし、特定製品の説明書から追加レベルや解答を転記してはいけません。任意拡張のために MVP の品質、読みやすさ、速度、アクセシビリティを犠牲にしないでください。

---

## 15. 作業手順

1. リポジトリ構成と既存 GitHub Pages の作りを確認する。
2. 変更範囲を `sliding-block-puzzle/` に限定できることを確認する。
3. 短い実装方針とファイル構成を示す。
4. レベル定義、盤面モデル、バリデータ、合法移動、テストを先に実装する。
5. レスポンシブな盤面描画を実装する。
6. Pointer Events、タップ操作、キーボード操作を実装する。
7. 履歴、保存、タイマー、クリア UI を実装する。
8. Web Worker ソルバー、ヒント、解答再生を実装する。
9. 自動テストを実行し、失敗を修正する。
10. ローカル HTTP サーバーで `/sliding-block-puzzle/` を確認する。
11. 可能な範囲で、狭い画面、広い画面、キーボードのみ、Reduced Motion を確認する。
12. README を完成させる。
13. 参考用の画像 3 点と PDF 1 点を削除し、原本・変換物・派生物が Git の追跡対象や push 対象に残っていないことを確認する。
14. 最終報告を行う。

---

## 16. 最終報告の形式

作業後は、次の順で簡潔に報告してください。

1. 実装した内容。
2. 変更・追加したファイル。
3. 実行したテストと結果。
4. ローカル起動方法。
5. GitHub Pages での確認 URL。
6. 著作権・商標配慮として、公式素材、固有レベル、固有解答を含めていないこと。
7. 参考用の画像 3 点と PDF 1 点を、公開物へ転用せず `git push` 前に削除したこと。
8. 既知の制約または未実施の任意拡張。

雛形だけを作って終了せず、実際に遊べ、ヒントが動き、テストが通る状態まで完成させてください。
